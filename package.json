{
  "name": "cadus",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:all": "bun --filter '*' dev",
    "test": "vitest",
    "test:all": "vitest && bun --filter @cadus/e2e test",
    "typecheck": "tsc --build",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cors": "^2.8.6"
  }
,
"overrides": {
  "fast-check": "3.22.0"
}
}
